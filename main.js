const sum = (num1, num2) => num1 + num2;

const subtrect = (num1, num2) => num1 - num2;

const multiply = (num1, num2) => num1 * num2;

const devide = (num1, num2) => num1 / num2;

module.exports  = {sum, subtrect, multiply, devide};